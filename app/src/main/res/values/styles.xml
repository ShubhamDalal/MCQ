<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Questions">
        <!-- Question Style. -->
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="SpaceView">
        <!-- Space between Question and Question number. -->
        <item name="android:layout_marginLeft">@dimen/spaceViewMargin</item>
        <item name="android:layout_marginRight">@dimen/spaceViewMargin</item>
        <item name="android:background">@android:color/black</item>
        <item name="android:layout_width">@dimen/spaceViewWidth</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="CardView">
        <!-- CardView Style. -->
        <item name="android:layout_margin">@dimen/cardViewMargin</item>
        <item name="android:background">@drawable/rounded_corner</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:elevation">@dimen/elevationSize</item>
    </style>

    <style name="OptionsLayout">
        <!-- Layout style for options. -->
        <item name="android:layout_marginLeft">@dimen/optionsMargin</item>
        <item name="android:layout_marginBottom">@dimen/optionsMargin</item>
        <item name="android:layout_marginRight">@dimen/optionsMargin</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>
